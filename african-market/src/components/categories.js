

// SAME PAGE FOR ITEMS AND CATEGORIES
function Browse(props) {
    let {categories, items, setItems} = props;

    // CHECK TO SEE IF ITEMS IS TRUE/FALSE
    // IF TRUE RUN FUNCTION-A TO RENDER ITEMS TO PAGE IN CARDS
    // IF FALSE RUN FUNCTION-A TO RENDER CATEGORIES TO PAGE IN CARDS

    return (
        <h2>browse page</h2>
    )
}

export default Browse;